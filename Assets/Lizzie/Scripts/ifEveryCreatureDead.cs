using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IfEveryCreatureDead : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	
	void AreCreaturesDead(int numCreatures) {
		if (numCreatures == 0) {
			// spawn new creature
			// or game over depending on our preferences
		}
	}
	
}
